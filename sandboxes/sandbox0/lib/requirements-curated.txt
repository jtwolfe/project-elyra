# Curated guest Python packages for sandbox0 (H3b / KD11 / KD22).
# Installed into the warm guest after mount readiness when network allows.
# Marker: tmp/.elyra_pyenv_ready (requirements hash).
#
# MUST include pytest — isolation-on verify_tool runs guest pytest.
# Pin major versions; operators may extend this list.

# Verify (required)
pytest>=8,<9

# Common tool-author libraries (light; no ML stacks).
# Prefer pure-Python / wheels only — guest images often lack build deps
# (gcc, libxml2-dev). Do NOT pin packages that require compiling C
# extensions without manylinux wheels (e.g. lxml). BeautifulSoup uses
# stdlib html.parser when lxml is absent.
requests>=2,<3
httpx>=0.27,<1
beautifulsoup4>=4,<5
pyyaml>=6,<7
python-dateutil>=2,<3
regex>=2024.0
jinja2>=3,<4
