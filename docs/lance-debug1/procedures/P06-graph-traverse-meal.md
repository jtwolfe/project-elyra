# P06 — Graph / traverse / meal

| Field | Value |
|-------|--------|
| **Status** | Stub (filled in PR4) |
| **Safety class** | R2 + optional offline harness (R1) |
| **Prove / disprove** | H8, H11, H12; bucket E |
| **Script (optional)** | `consumer_compare.py` (PR4) |

## Purpose

Document consumer behavior under thin vs full corpus. **No patched product `_load`.**

## Notes

- Split glass newest-first haiku tools from raw `to_arrow` table-order prefix.
- Expand_ms overruns → H11 / BUG-mem-gpu-01 adjacency, not root of missing atoms.
