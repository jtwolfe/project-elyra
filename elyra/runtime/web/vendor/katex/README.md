# KaTeX (vendored)

Version: **0.16.21** (from [KaTeX releases](https://github.com/KaTeX/KaTeX/releases)).

Used by Glass chat / Moments prose (`renderMarkdown` in `app.js`) for BUG-chat-01.

Do not load from a CDN — offline dogfood and same-origin static serving under `/vendor/katex/`.
