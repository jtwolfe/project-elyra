{
  "type": "object",
  "properties": {
    "session_id": {
      "type": "string",
      "description": "Session id from browser_session_open"
    },
    "ref": {
      "type": "string",
      "description": "Element ref from the last browser_snapshot (e.g. e3)"
    }
  },
  "required": [
    "session_id",
    "ref"
  ],
  "additionalProperties": false
}
