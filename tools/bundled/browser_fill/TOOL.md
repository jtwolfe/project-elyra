---
name: browser_fill
description: Fill (replace) text in an element by snapshot ref.
kind: mutate
---

# browser_fill

Replace the value of the element at `ref` with `text`. Use after a fresh
`browser_snapshot`. Prefer over `browser_type` for form fields.

