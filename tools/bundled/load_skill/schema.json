{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Skill package name from orient catalog (talk, plan-work, do-work, review-work, rest, create-tool, create-skill)"
    }
  },
  "required": ["name"],
  "additionalProperties": false
}
