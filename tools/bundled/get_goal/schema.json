{
  "type": "object",
  "properties": {
    "goal_id": {
      "type": "string",
      "description": "Id of the goal to fetch (e.g. g_…)"
    }
  },
  "required": ["goal_id"],
  "additionalProperties": false
}
