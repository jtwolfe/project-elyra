---
name: list_dir
description: List names in a sandbox directory (not recursive). Default path is ".".
kind: read
---

# list_dir

List entry names under a directory in the persistent sandbox. Path is jailed;
escapes return a tool error.
